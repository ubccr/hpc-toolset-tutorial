version: "3.8"

services:
  base:
    image: hpctk-base:$HPCTK_VERSION
    build:
      context: ./base

  mysql:
    image: mariadb:$MARIADB_VERSION
    hostname: mysql
    container_name: mysql
    environment:
      MYSQL_ALLOW_EMPTY_PASSWORD: "yes"
    networks:
      - compute
    volumes:
      - ./database:/docker-entrypoint-initdb.d
      - ./slurm/slurmdbd.conf:/etc/slurm/slurmdbd.conf
      - var_lib_mysql:/var/lib/mysql

  slurmdbd:
    image: hpctk-slurm:$HPCTK_VERSION
    build:
      context: ./slurm
      args:
        SLURM_VERSION: $SLURM_VERSION
        HPCTK_VERSION: $HPCTK_VERSION
    command: ["slurmdbd"]
    container_name: slurmdbd
    hostname: slurmdbd
    networks:
      - compute
    volumes:
      - etc_munge:/etc/munge
      - etc_slurm:/etc/slurm
    expose:
      - "6819"
    depends_on:
      - base
      - mysql

  slurmctld:
    image: hpctk-slurm:$HPCTK_VERSION
    command: ["slurmctld"]
    container_name: slurmctld
    hostname: slurmctld
    networks:
      - compute
    volumes:
      - etc_munge:/etc/munge
      - etc_slurm:/etc/slurm
      - home:/home
    expose:
      - "6817"
    depends_on:
      - slurmdbd

  c1:
    image: hpctk-slurm:$HPCTK_VERSION
    command: ["slurmd"]
    hostname: c1
    container_name: c1
    networks:
      - compute
    volumes:
      - etc_munge:/etc/munge
      - etc_slurm:/etc/slurm
      - home:/home
    expose:
      - "6818"
    depends_on:
      - slurmctld

  c2:
    image: hpctk-slurm:$HPCTK_VERSION
    command: ["slurmd"]
    hostname: c2
    container_name: c2
    networks:
      - compute
    volumes:
      - etc_munge:/etc/munge
      - etc_slurm:/etc/slurm
      - home:/home
    expose:
      - "6818"
    depends_on:
      - slurmctld

  frontend:
    image: hpctk-slurm:$HPCTK_VERSION
    command: ["frontend"]
    hostname: frontend
    container_name: frontend
    networks:
      - compute
    volumes:
      - etc_munge:/etc/munge
      - etc_slurm:/etc/slurm
      - home:/home
    expose:
      - "22"
    depends_on:
      - slurmctld

  coldfront:
    image: hpctk-coldfront:$HPCTK_VERSION
    build:
      context: ./coldfront
      args:
        HPCTK_VERSION: $HPCTK_VERSION
    hostname: coldfront
    container_name: coldfront
    networks:
      - compute
    volumes:
      - etc_munge:/etc/munge
      - etc_slurm:/etc/slurm
      - home:/home
      - srv_www:/srv/www
    expose:
      - "22"
      - "80"
      - "443"
    depends_on:
      - mysql
      - frontend

  ondemand:
    image: hpctk-ondemand:$HPCTK_VERSION
    build:
      context: ./ondemand
      args:
        HPCTK_VERSION: $HPCTK_VERSION
    hostname: ondemand
    container_name: ondemand
    networks:
      - compute
    volumes:
      - etc_munge:/etc/munge
      - etc_slurm:/etc/slurm
      - home:/home
    expose:
      - "22"
      - "80"
      - "443"
    depends_on:
      - frontend

volumes:
  etc_munge:
  etc_slurm:
  home:
  var_lib_mysql:
  srv_www:

networks:
  compute:
